<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp">


    <ImageView
        android:id="@+id/films_image"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_marginBottom="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:srcCompat="@tools:sample/avatars"
        android:adjustViewBounds="true"
        android:scaleType="centerCrop"
        app:srcCompat="@drawable/ic_launcher_background"/>

    <TextView
        android:padding="8dp"
        android:id="@+id/films_name"
        android:layout_width="0dp"
        android:layout_height="65dp"
        android:textColor="@color/colorBlack"
        android:background="@color/colorTransparentForText"
        android:gravity="center"
        android:maxLines="2"
        android:ellipsize="end"
        android:text="TextView"
        app:layout_constraintBottom_toBottomOf="@+id/films_image"
        app:layout_constraintEnd_toEndOf="@+id/films_image"
        app:layout_constraintStart_toStartOf="@+id/films_image" />
</androidx.constraintlayout.widget.ConstraintLayout>